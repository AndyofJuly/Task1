<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.game.dao.RolesMapper">
    <select id="checkRolename" resultType="int" parameterType="string">
        select count(1) from role
        where rolename=#{rolename}
    </select>

    <insert id="insertRegisterRole" parameterType="map">
        insert into role(rolename,placeid,alive,careerid)
        values (#{rolename,jdbcType=VARCHAR},#{placeid,jdbcType=INT},#{alive,jdbcType=INT},#{careerid,jdbcType=INT})
    </insert>

    <select id="selectLoginRole" resultType="int" parameterType="map">
        select careerid from role
        where rolename=#{rolename}
    </select>>

    <update id="insertRoleScenes" parameterType="map">
        update role
        set placeid=#{placeid,jdbcType=INT}
        where rolename=#{rolename,jdbcType=VARCHAR}
    </update>

    <select id="selectRoleIdByName" resultType="int" parameterType="string">
        select playid from role
        where rolename=#{rolename}
    </select>

    <select id="selectRoleScenesId" resultType="int" parameterType="string">
        select placeid from role
        where rolename=#{rolename}
    </select>

</mapper>